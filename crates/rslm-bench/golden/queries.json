[
  {
    "id": "conn_limit",
    "query": "How does mini-redis limit the number of concurrent connections? Search for Semaphore and MAX_CONNECTIONS.",
    "expected_patterns": ["Semaphore", "MAX_CONNECTIONS"],
    "harness_responses": [
      "let r = ctx_grep(\"Semaphore\"); final_answer(r)"
    ]
  },
  {
    "id": "shutdown_server",
    "query": "How does the server signal active connections to shut down? Search for notify_shutdown and broadcast.",
    "expected_patterns": ["broadcast", "notify_shutdown"],
    "harness_responses": [
      "let r = ctx_grep(\"notify_shutdown\"); final_answer(r)"
    ]
  },
  {
    "id": "shutdown_wait",
    "query": "What mechanism does the server use to wait for active connections to finish before exiting? Search for shutdown_complete.",
    "expected_patterns": ["shutdown_complete"],
    "harness_responses": [
      "let r = ctx_grep(\"shutdown_complete\"); final_answer(r)"
    ]
  },
  {
    "id": "db_expiry_structures",
    "query": "What data structures does the Db use to track key expiration? Search for BTreeSet and expirations.",
    "expected_patterns": ["BTreeSet", "expirations"],
    "harness_responses": [
      "let r = ctx_grep(\"BTreeSet\"); final_answer(r)"
    ]
  },
  {
    "id": "db_expiry_notify",
    "query": "How does the Db notify the background task when a new expiration is added? Search for notify_one.",
    "expected_patterns": ["notify_one"],
    "harness_responses": [
      "let r = ctx_grep(\"notify_one\"); final_answer(r)"
    ]
  },
  {
    "id": "frame_types",
    "query": "What are the frame types in the Frame enum? Search for 'Frame::' to find enum variants.",
    "expected_patterns": ["Simple", "Bulk", "Integer"],
    "harness_responses": [
      "let r = ctx_grep(\"Frame::\"); final_answer(r)"
    ]
  },
  {
    "id": "connection_buffer",
    "query": "What is the default read buffer size used by the Connection struct? Search for 'with_capacity' to find the buffer initialization.",
    "expected_patterns": ["4 * 1024"],
    "harness_responses": [
      "let r = ctx_grep(\"with_capacity\"); final_answer(r)"
    ]
  },
  {
    "id": "backoff_strategy",
    "query": "What backoff strategy does the server use when accepting connections fails? Search for backoff and exponential.",
    "expected_patterns": ["backoff", "exponential"],
    "harness_responses": [
      "let r = ctx_grep(\"backoff\"); final_answer(r)"
    ]
  },
  {
    "id": "pubsub_capacity",
    "query": "What is the broadcast channel capacity used for pub/sub channels? Search for broadcast::channel.",
    "expected_patterns": ["broadcast::channel", "1024"],
    "harness_responses": [
      "let r = ctx_grep(\"broadcast::channel\"); final_answer(r)"
    ]
  },
  {
    "id": "db_mutex_choice",
    "query": "Why does the Db use std::sync::Mutex instead of tokio::sync::Mutex? Search for 'not a Tokio mutex' to find the explanation.",
    "expected_patterns": ["not a Tokio mutex"],
    "harness_responses": [
      "let r = ctx_grep(\"not a Tokio mutex\"); final_answer(r)"
    ]
  }
]
